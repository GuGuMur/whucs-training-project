\documentclass[a4paper,zihao=-4]{ctexart}

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{etoolbox}
\usepackage{float}
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{array}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{adjustbox}
\usepackage{listings}
\usepackage[top=2.54cm,bottom=2.54cm,left=2cm,right=2cm,
  headheight=24pt,headsep=8mm]{geometry}
\usepackage{fancyhdr}
\usepackage[output=png]{plantuml}
\usepackage{hyperref}

\definecolor{codebg}{HTML}{F6F8FB}
\definecolor{codeframe}{HTML}{D8E0EA}
\definecolor{codetext}{HTML}{172033}
\definecolor{codekeyword}{HTML}{246BFE}
\definecolor{codestring}{HTML}{1F9D55}
\definecolor{codecomment}{HTML}{5D6B82}

\lstdefinestyle{systemdesign}{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small\color{codetext},
  keywordstyle=\color{codekeyword},
  stringstyle=\color{codestring},
  commentstyle=\color{codecomment},
  frame=single,
  rulecolor=\color{codeframe},
  framerule=0.4pt,
  breaklines=true,
  breakatwhitespace=false,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  tabsize=2,
  xleftmargin=0.6em,
  xrightmargin=0.6em,
  aboveskip=0.8em,
  belowskip=0.8em
}
\lstset{style=systemdesign}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue,
}

\setlist{nosep}
\setlength{\parindent}{2em}
\setlength{\parskip}{0.25em}
\setlength{\emergencystretch}{4em}
\setlength{\LTpre}{4pt}
\setlength{\LTpost}{8pt}
\renewcommand{\arraystretch}{1.16}
\AtBeginEnvironment{longtable}{\small}
\captionsetup[figure]{font=small,skip=6pt}
\captionsetup[table]{font=small,skip=6pt}

\graphicspath{{assets/}{../assets/}{../../assets/}}

\fancypagestyle{frontpage}{
  \fancyhf{}
  \fancyfoot[C]{\zihao{5}\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

\fancypagestyle{contentpage}{
  \fancyhf{}
  \fancyhead[L]{\zihao{5} 基于大模型的智能文件管理与智能体协同平台 · 系统设计说明书}
  \fancyhead[R]{\includegraphics[height=18pt]{whu.png}}
  \fancyfoot[C]{\zihao{5}\thepage}
  \renewcommand{\headrulewidth}{0.5pt}
}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\passthrough}[1]{#1}

\begin{document}

\thispagestyle{empty}
\begin{center}
  \vspace*{2cm}
  \includegraphics[height=5cm]{whu.png}

  \vspace{1.5cm}
  {\heiti\zihao{1} 基于大模型的智能文件管理与智能体协同平台系统设计说明书}

  \vspace{2cm}
  {\zihao{3}\textbf{项目文档}}

  \vspace{2cm}
  {\zihao{4} 2026年7月}
\end{center}
\newpage

\pagestyle{frontpage}
\tableofcontents
\newpage

\pagestyle{contentpage}

\hypertarget{ux5f15ux8a00}{%
\section{引言}\label{ux5f15ux8a00}}

\hypertarget{ux7f16ux5199ux76eeux7684}{%
\subsection{编写目的}\label{ux7f16ux5199ux76eeux7684}}

本文档在《需求规格说明书》和现有
UML/开发计划文档基础上，对``基于大模型的智能文件管理与智能体协同平台''进行系统设计说明。文档面向项目开发、测试、部署和答辩评审，覆盖系统体系架构、功能结构、关键用例时序、复杂算法、类图、接口、数据库物理设计、UI
设计以及前后端依赖库选型。

\hypertarget{ux7cfbux7edfux5b9aux4f4d}{%
\subsection{系统定位}\label{ux7cfbux7edfux5b9aux4f4d}}

系统以文件管理为基础，以知识检索为能力核心，以智能体和可视化工具流作为自动化执行入口，并通过团队空间、权限和协作机制支撑多人学习、课程小组和科研团队场景。系统不是单纯网盘，也不是单轮
AI 聊天工具，而是面向文件、知识、流程和团队的智能协作平台。

\hypertarget{ux7cfbux7edfux4f53ux7cfbux67b6ux6784}{%
\section{系统体系架构}\label{ux7cfbux7edfux4f53ux7cfbux67b6ux6784}}

\hypertarget{ux603bux4f53ux67b6ux6784}{%
\subsection{总体架构}\label{ux603bux4f53ux67b6ux6784}}

系统采用前后端分离、领域模块化、异步任务驱动的架构。前端负责文件工作台、知识问答、流程画布和团队协作界面；后端通过
FastAPI 提供 REST API、WebSocket、异步任务调度和智能体编排；存储层由
MySQL、对象存储、向量索引共同构成。

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-01}
\begin{plantuml}
@startuml
title 系统体系架构
skinparam componentStyle rectangle

actor "用户/团队成员/管理员" as User

package "Web 前端层\nVue 3 + Vite + TypeScript" {
  [文件管理工作台] as FEFile
  [RAG 问答界面] as FEQA
  [工具流编排器\nVue Flow] as FEWorkflow
  [团队协作界面] as FETeam
  [管理员后台] as FEAdmin
}

package "API 与实时通信层\nFastAPI" {
  [REST API Router] as REST
  [WebSocket Gateway] as WS
  [JWT 鉴权中间件] as AuthMiddleware
  [RBAC 权限中间件] as RbacMiddleware
}

package "领域服务层" {
  [用户认证服务] as Auth
  [文件管理服务] as FileSvc
  [知识库/RAG 服务] as RAGSvc
  [Agent 编排服务] as AgentSvc
  [工具流执行服务] as WorkflowSvc
  [团队协作服务] as TeamSvc
  [权限审计服务] as AuditSvc
}

package "AI 与异步任务层" {
  [文档解析任务] as ParseJob
  [Embedding 任务] as EmbeddingJob
  [OCR 服务] as OCR
  [LLM 网关] as LLM
  [任务队列\nARQ/Celery + Redis] as Queue
}

database "MySQL 8.0\n元数据/权限/消息/日志" as MySQL
database "MinIO/本地文件系统\n文件实体与版本" as ObjectStore
database "FAISS/Milvus\n向量索引" as VectorDB
cloud "DeepSeek/OpenAI 兼容 LLM API" as ExternalLLM

User --> FEFile
User --> FEQA
User --> FEWorkflow
User --> FETeam
User --> FEAdmin

FEFile --> REST
FEQA --> REST
FEQA --> WS
FEWorkflow --> REST
FEWorkflow --> WS
FETeam --> REST
FETeam --> WS
FEAdmin --> REST

REST --> AuthMiddleware
AuthMiddleware --> RbacMiddleware
RbacMiddleware --> Auth
RbacMiddleware --> FileSvc
RbacMiddleware --> RAGSvc
RbacMiddleware --> AgentSvc
RbacMiddleware --> WorkflowSvc
RbacMiddleware --> TeamSvc
RbacMiddleware --> AuditSvc

FileSvc --> ObjectStore
FileSvc --> MySQL
FileSvc --> Queue
Queue --> ParseJob
ParseJob --> OCR
ParseJob --> EmbeddingJob
EmbeddingJob --> VectorDB

RAGSvc --> VectorDB
RAGSvc --> MySQL
RAGSvc --> LLM
AgentSvc --> LLM
AgentSvc --> FileSvc
AgentSvc --> RAGSvc
WorkflowSvc --> AgentSvc
WorkflowSvc --> MySQL
TeamSvc --> MySQL
AuditSvc --> MySQL
WS --> MySQL
LLM --> ExternalLLM
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{系统体系架构}
\end{figure}

\hypertarget{ux67b6ux6784ux5206ux5c42ux8bf4ux660e}{%
\subsection{架构分层说明}\label{ux67b6ux6784ux5206ux5c42ux8bf4ux660e}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
层次 & 主要职责 & 关键技术\tabularnewline
\hline
\endhead
表现层 & 提供文件、知识库、问答、流程画布、团队协作、后台管理等页面 & Vue 3、TypeScript、Naive UI、Vue Flow、Pinia\tabularnewline
\hline
API 层 & 暴露 REST API、WebSocket、鉴权、权限检查、请求校验 & FastAPI、Pydantic、JWT\tabularnewline
\hline
领域服务层 & 封装用户、文件、RAG、Agent、工具流、团队、审计等业务逻辑 & Python service 模块、SQLAlchemy\tabularnewline
\hline
AI 能力层 & 文档解析、OCR、Embedding、语义检索、重排序、LLM 生成、Agent 推理 & LangChain、LangGraph、FAISS/Milvus、PaddleOCR\tabularnewline
\hline
异步任务层 & 将解析、向量化、工作流执行等耗时任务从请求链路中剥离 & Redis、ARQ/Celery\tabularnewline
\hline
存储层 & 保存元数据、权限、消息、日志、文件实体和向量数据 & MySQL、MinIO、本地文件系统、FAISS/Milvus\tabularnewline
\hline
\end{longtable}

\hypertarget{ux5173ux952eux67b6ux6784ux539fux5219}{%
\subsection{关键架构原则}\label{ux5173ux952eux67b6ux6784ux539fux5219}}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{权限前置}：所有文件、知识库、团队、工具和流程资源访问都必须先经过
  JWT 和 RBAC 校验。
\item
  \textbf{上传与解析解耦}：文件上传成功后立即返回基础元数据，解析、OCR
  和向量化进入异步任务队列。
\item
  \textbf{AI 能力可降级}：LLM、Embedding 或 OCR
  不可用时，文件管理、权限、团队协作和已建索引检索仍可使用。
\item
  \textbf{工具可扩展}：工具通过 JSON
  Schema、调用类型和调用配置注册，不修改 Agent
  核心执行器即可增加新工具。
\item
  \textbf{向量库可替换}：MVP 使用
  FAISS，本地索引满足轻量部署；数据规模增长后通过统一 VectorStore
  接口迁移到 Milvus。
\end{enumerate}

\hypertarget{ux7cfbux7edfux529fux80fdux7ed3ux6784}{%
\section{系统功能结构}\label{ux7cfbux7edfux529fux80fdux7ed3ux6784}}

\hypertarget{ux529fux80fdux5c42ux6b21ux7ed3ux6784}{%
\subsection{功能层次结构}\label{ux529fux80fdux5c42ux6b21ux7ed3ux6784}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-02}
\begin{plantuml}
@startuml
title 系统功能层次结构图
skinparam componentStyle rectangle
skinparam shadowing false
skinparam defaultFontName "Noto Sans CJK SC"
left to right direction

package "智能文件管理与智能体协同平台" {
  package "用户与认证" {
    [注册/登录]
    [JWT Token 签发与刷新]
    [个人资料维护]
    [登录失败锁定]
  }

  package "文件管理" {
    [文件夹树与面包屑]
    [上传/分片上传/断点续传]
    [下载/移动/复制/重命名/删除]
    [文件版本控制]
    [标签与筛选]
    [分享链接]
    [回收站与备份]
  }

  package "知识库与 RAG" {
    [知识库创建与权限配置]
    [多格式文档解析]
    [OCR 文本提取]
    [文本分块与向量化]
    [Top-K 召回与 Reranker]
    [多轮问答]
    [引用溯源]
  }

  package "智能体编排" {
    [工具注册中心]
    [工具 Schema 校验]
    [ReAct 执行循环]
    [工具调用日志]
    [超时/重试/降级]
    [最终结果生成]
  }

  package "可视化工具流" {
    [Vue Flow 流程编辑器]
    [工具/条件/聚合/输出节点]
    [参数绑定]
    [DAG 校验]
    [执行调度]
    [单步调试]
    [执行历史]
  }

  package "团队协作" {
    [团队创建与邀请]
    [成员角色]
    [团队文件夹]
    [群聊/私聊]
    [文件批注与回复]
    [活动动态]
    [通知推送]
  }

  package "权限与审计" {
    [系统角色]
    [团队角色]
    [资源级 ACL]
    [权限继承]
    [工具执行权限]
    [审计日志]
  }
}
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{系统功能层次结构图}
\end{figure}

\hypertarget{ux6a21ux5757ux804cux8d23ux5212ux5206}{%
\subsection{模块职责划分}\label{ux6a21ux5757ux804cux8d23ux5212ux5206}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-8\tabcolsep-5\arrayrulewidth)/3\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-8\tabcolsep-5\arrayrulewidth)/3\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-8\tabcolsep-5\arrayrulewidth)/3\relax}|}
\hline
模块 & 输入 & 输出 & 说明\tabularnewline
\hline
\endhead
用户认证模块 & 注册信息、登录凭证、Refresh Token & 用户信息、Access Token、权限上下文 & 统一身份入口\tabularnewline
\hline
文件管理模块 & 文件实体、文件夹操作、标签、分享配置 & 文件元数据、下载链接、上传状态 & 平台基础数据入口\tabularnewline
\hline
知识库模块 & 已上传文件、知识库配置、自然语言问题 & 文档索引、检索片段、问答结果 & 支撑跨文档语义检索\tabularnewline
\hline
Agent 模块 & 用户复杂任务、工具定义、上下文 & 工具调用步骤、最终答案或报告 & 支撑多步骤任务自动执行\tabularnewline
\hline
工具流模块 & 流程节点、连线、参数、触发输入 & 流程执行记录、节点输出、进度事件 & 支撑可视化自动化\tabularnewline
\hline
团队协作模块 & 团队、成员、消息、批注、通知事件 & 团队空间、消息流、通知、动态 & 支撑多人协同\tabularnewline
\hline
权限审计模块 & 用户身份、资源、动作、操作详情 & 允许/拒绝结果、审计日志 & 横切所有业务模块\tabularnewline
\hline
\end{longtable}

\hypertarget{ux7cfbux7edfux7528ux4f8bux65f6ux5e8fux56feux53caux8bf4ux660e}{%
\section{系统用例时序图及说明}\label{ux7cfbux7edfux7528ux4f8bux65f6ux5e8fux56feux53caux8bf4ux660e}}

\hypertarget{ux4e2aux4eba-rag-ux95eeux7b54ux65f6ux5e8fux56fe}{%
\subsection{个人 RAG
问答时序图}\label{ux4e2aux4eba-rag-ux95eeux7b54ux65f6ux5e8fux56fe}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-03}
\begin{plantuml}
@startuml
title 个人 RAG 问答时序图
actor 用户 as User
participant "Web 前端" as FE
participant "FastAPI API" as API
participant "权限服务" as Permission
participant "知识库服务" as KB
participant "向量数据库" as VDB
participant "Reranker" as Reranker
participant "LLM 网关" as LLM
database "MySQL" as DB

User -> FE: 输入自然语言问题
FE -> API: POST /api/v1/qa/query
API -> Permission: 校验 JWT、知识库、文件访问权限
Permission -> DB: 查询用户角色与资源 ACL
DB --> Permission: 权限规则
Permission --> API: 允许访问
API -> KB: 创建问答请求
KB -> DB: 读取会话历史、知识库配置
DB --> KB: 历史消息与配置
KB -> VDB: Query Embedding + Top-K 召回
VDB --> KB: 候选片段
KB -> Reranker: 候选片段精排
Reranker --> KB: 排序后的引用片段
KB -> LLM: Prompt + 片段 + 会话历史
LLM --> KB: 流式答案
KB -> DB: 保存用户问题、答案、引用来源
KB --> API: 答案流 + citation 列表
API --> FE: SSE/WebSocket 返回
FE --> User: 展示答案、引用和源文档跳转
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{个人 RAG 问答时序图}
\end{figure}

\textbf{说明：}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  前端提交问题时必须携带 \passthrough{\lstinline!conversation\_id!} 或
  \passthrough{\lstinline!kb\_id!}，后端根据用户身份和资源 ACL
  判断是否允许访问。
\item
  RAG 服务先读取会话历史，再对问题生成向量并从 FAISS/Milvus 召回 Top-K
  片段。
\item
  Reranker 对召回片段进行精排，减少语义召回中的低相关片段。
\item
  LLM 输出采用流式返回，前端边接收边展示，最终答案保存到
  \passthrough{\lstinline!conversation\_messages!}。
\item
  引用来源以
  \passthrough{\lstinline!document\_id!}、\passthrough{\lstinline!chunk\_id!}、页码、段落位置组成，支持前端跳转源文档。
\end{enumerate}

\hypertarget{ux6587ux4ef6ux4e0aux4f20ux4e0eux77e5ux8bc6ux5e93ux5165ux5e93ux65f6ux5e8fux56fe}{%
\subsection{文件上传与知识库入库时序图}\label{ux6587ux4ef6ux4e0aux4f20ux4e0eux77e5ux8bc6ux5e93ux5165ux5e93ux65f6ux5e8fux56fe}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-04}
\begin{plantuml}
@startuml
title 文件上传与知识库入库时序图
actor 用户 as User
participant "Web 前端" as FE
participant "文件 API" as FileAPI
participant "权限服务" as Permission
participant "文件服务" as FileSvc
participant "对象存储" as ObjectStore
participant "任务队列" as Queue
participant "解析任务" as Parser
participant "Embedding 任务" as Embedder
database "MySQL" as DB
database "向量数据库" as VDB

User -> FE: 选择文件并上传
FE -> FileAPI: POST /api/v1/files/upload
FileAPI -> Permission: 校验目标文件夹写权限
Permission --> FileAPI: 允许
FileAPI -> FileSvc: 保存文件元数据
FileSvc -> ObjectStore: 写入文件实体
ObjectStore --> FileSvc: storage_key
FileSvc -> DB: 写入 files 与 file_versions
FileSvc -> Queue: 投递 document_parse 任务
FileSvc --> FileAPI: 返回上传成功和解析状态 pending
FileAPI --> FE: 文件出现在列表中

Queue -> Parser: 执行解析任务
Parser -> ObjectStore: 读取文件内容
Parser -> DB: 写入 documents, content_text
Parser -> Embedder: 分块与向量化
Embedder -> VDB: 写入 chunk embedding
Embedder -> DB: 写入 document_chunks 与 embedding_id
Embedder -> FE: WebSocket 推送解析成功
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{文件上传与知识库入库时序图}
\end{figure}

\textbf{说明：}

上传主链路只负责权限、存储、元数据和任务投递，不等待文档解析完成。解析失败不会影响文件基础管理，但会将
\passthrough{\lstinline!documents.parse\_status!} 标记为
\passthrough{\lstinline!failed!} 并记录错误信息。

\hypertarget{agent-react-ux5de5ux5177ux8c03ux7528ux65f6ux5e8fux56fe}{%
\subsection{Agent ReAct
工具调用时序图}\label{agent-react-ux5de5ux5177ux8c03ux7528ux65f6ux5e8fux56fe}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-05}
\begin{plantuml}
@startuml
title Agent ReAct 工具调用时序图
actor 用户 as User
participant "Web 前端" as FE
participant "Agent 服务" as Agent
participant "工具注册中心" as Registry
participant "LLM 网关" as LLM
participant "文件搜索工具" as FileSearch
participant "知识问答工具" as KnowledgeQA
participant "报告生成工具" as Report
database "执行日志库" as LogDB

User -> FE: 输入复杂任务
FE -> Agent: POST /api/v1/agents/tasks
Agent -> Registry: 获取用户可执行工具及 Schema
Registry --> Agent: 工具列表
Agent -> LLM: 用户任务 + 工具描述 + 当前上下文
LLM --> Agent: Thought + Action(file_search)
Agent -> FileSearch: 参数校验并调用
FileSearch --> Agent: Observation: 文件列表
Agent -> LogDB: 保存步骤日志
Agent -> LLM: Observation + 下一步提示
LLM --> Agent: Thought + Action(knowledge_qa)
Agent -> KnowledgeQA: 调用 RAG 问答
KnowledgeQA --> Agent: Observation: 摘要和引用
Agent -> LogDB: 保存步骤日志
Agent -> LLM: 汇总上下文
LLM --> Agent: Action(report_generate)
Agent -> Report: 生成报告
Report --> Agent: 报告内容
Agent -> LogDB: 保存最终结果
Agent --> FE: 返回执行过程和结果
FE --> User: 展示步骤、工具输出、最终报告
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{Agent ReAct 工具调用时序图}
\end{figure}

\textbf{说明：}

Agent 执行遵循 ReAct
模式：\passthrough{\lstinline!Thought -> Action -> Observation -> Answer!}。每次工具调用前进行
JSON Schema
参数校验、权限校验和超时控制；每次调用结果写入执行日志，便于前端展示和后续审计。

\hypertarget{ux56e2ux961fux5468ux62a5ux5de5ux5177ux6d41ux6267ux884cux65f6ux5e8fux56fe}{%
\subsection{团队周报工具流执行时序图}\label{ux56e2ux961fux5468ux62a5ux5de5ux5177ux6d41ux6267ux884cux65f6ux5e8fux56fe}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-06}
\begin{plantuml}
@startuml
title 团队周报工具流执行时序图
actor "组长" as Leader
participant "工具流编辑器" as FE
participant "工作流 API" as API
participant "工作流引擎" as Engine
participant "权限服务" as Permission
participant "节点执行器" as NodeRunner
participant "Agent/RAG 服务" as AgentRAG
participant "WebSocket" as WS
database "MySQL" as DB

Leader -> FE: 选择“团队周报生成”模板并执行
FE -> API: POST /api/v1/workflows/{id}/executions
API -> Permission: 校验团队、文件夹、工具执行权限
Permission --> API: 允许
API -> Engine: 创建执行实例
Engine -> DB: 写入 workflow_executions
Engine -> Engine: 解析 nodes/edges 并 DAG 校验
Engine -> Engine: 拓扑排序
loop 每批可执行节点
  Engine -> NodeRunner: 执行节点
  NodeRunner -> AgentRAG: 调用文件扫描/内容提取/摘要工具
  AgentRAG --> NodeRunner: 节点输出
  NodeRunner -> DB: 写入 workflow_node_executions
  NodeRunner -> WS: 推送节点进度
  WS --> FE: 更新画布节点状态
end
Engine -> DB: 更新执行结果
Engine -> WS: 推送完成通知
FE --> Leader: 展示周报和执行历史
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{团队周报工具流执行时序图}
\end{figure}

\textbf{说明：}

工作流执行以 DAG
为基本模型。系统先校验流程结构，再按拓扑顺序调度节点；入度为 0
且依赖完成的节点可并行执行。节点状态通过 WebSocket
实时推送，支持失败重试、跳过或终止策略。

\hypertarget{ux590dux6742ux529fux80fdux7b97ux6cd5ux8bbeux8ba1}{%
\section{复杂功能算法设计}\label{ux590dux6742ux529fux80fdux7b97ux6cd5ux8bbeux8ba1}}

\hypertarget{ux6587ux6863ux89e3ux6790ux5206ux5757ux4e0eux5411ux91cfux5316ux7b97ux6cd5}{%
\subsection{文档解析、分块与向量化算法}\label{ux6587ux6863ux89e3ux6790ux5206ux5757ux4e0eux5411ux91cfux5316ux7b97ux6cd5}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-07}
\begin{plantuml}
@startuml
title 文档解析与向量化流程图
start
:接收 file_id;
:读取 files 元数据和对象存储文件;
if (文件类型支持?) then (是)
  :按类型选择解析器;
else (否)
  :标记 parse_status=failed;
  stop
endif
:提取文本、表格、页码、段落位置;
if (包含图片或扫描页?) then (是)
  :调用 OCR 提取图片文字;
endif
:合并文本并清洗空白/页眉页脚;
:按 chunk_size=512, overlap=128 滑动分块;
repeat
  :生成 chunk 元数据;
  :调用 Embedding 模型生成向量;
  :写入向量数据库;
  :写入 document_chunks;
repeat while (仍有 chunk?)
:更新 documents.parse_status=success;
:WebSocket 推送入库完成;
stop
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{文档解析与向量化流程图}
\end{figure}

伪码：

\begin{lstlisting}
function index_document(file_id, kb_id):
    file = file_repo.get(file_id)
    assert_supported(file.mime_type)

    raw = object_store.read(file.storage_key)
    parsed = parser_factory.for_type(file.mime_type).parse(raw)

    if parsed.has_images:
        ocr_text = ocr.extract(parsed.images)
        parsed.text = merge(parsed.text, ocr_text)

    normalized_text = normalize(parsed.text)
    chunks = sliding_window(normalized_text, chunk_size=512, overlap=128)

    document = document_repo.create(kb_id, file_id, status="processing")
    for index, chunk in enumerate(chunks):
        vector = embedding_model.embed(chunk.content)
        embedding_id = vector_store.upsert(
            vector=vector,
            metadata={
                "file_id": file_id,
                "document_id": document.id,
                "chunk_index": index,
                "page_no": chunk.page_no,
                "paragraph_no": chunk.paragraph_no
            }
        )
        chunk_repo.create(document.id, index, chunk, embedding_id)

    document_repo.mark_success(document.id, chunk_count=len(chunks))
\end{lstlisting}

\hypertarget{rag-ux95eeux7b54ux7b97ux6cd5}{%
\subsection{RAG 问答算法}\label{rag-ux95eeux7b54ux7b97ux6cd5}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-08}
\begin{plantuml}
@startuml
title RAG 问答算法流程图
start
:接收 query, kb_id, conversation_id;
:校验用户对知识库/文件权限;
:读取会话历史;
:生成 Query Embedding;
:向量 Top-K 召回候选片段;
:过滤无权限片段;
if (候选为空?) then (是)
  :返回无结果提示;
  stop
endif
:Reranker 精排;
:组装 Prompt;
:调用 LLM 流式生成;
:抽取/绑定引用来源;
:保存问答消息;
:返回答案和 citations;
stop
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{RAG 问答算法流程图}
\end{figure}

伪码：

\begin{lstlisting}
function answer_question(user, kb_id, conversation_id, query):
    permission.ensure(user, "knowledge_base", kb_id, "read")

    history = conversation_repo.load_window(conversation_id, limit=8)
    query_vector = embedding_model.embed(query)
    candidates = vector_store.search(kb_id, query_vector, top_k=30)

    allowed = []
    for chunk in candidates:
        if permission.can(user, "file", chunk.file_id, "read"):
            allowed.append(chunk)

    if allowed is empty:
        return no_result_answer()

    ranked = reranker.rank(query, allowed)[:8]
    prompt = prompt_builder.build_rag_prompt(query, history, ranked)

    answer_stream = llm.stream(prompt)
    answer = collect_and_forward(answer_stream)
    citations = build_citations(ranked)

    conversation_repo.append_user_message(conversation_id, query)
    conversation_repo.append_assistant_message(conversation_id, answer, citations)
    return answer, citations
\end{lstlisting}

\hypertarget{ux5de5ux5177ux6d41-dag-ux6821ux9a8cux4e0eux8c03ux5ea6ux7b97ux6cd5}{%
\subsection{工具流 DAG
校验与调度算法}\label{ux5de5ux5177ux6d41-dag-ux6821ux9a8cux4e0eux8c03ux5ea6ux7b97ux6cd5}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-09}
\begin{plantuml}
@startuml
title 工具流 DAG 校验与执行流程图
start
:读取 workflow nodes/edges;
:校验节点引用和参数 Schema;
:构建邻接表与入度表;
if (存在孤立非法节点或缺失参数?) then (是)
  :返回定义错误;
  stop
endif
:执行 Kahn 拓扑排序;
if (排序数量 < 节点数量?) then (是)
  :返回存在环错误;
  stop
endif
:创建执行实例;
while (存在待执行节点?) is (是)
  :选择依赖已完成节点;
  fork
    :执行节点 A;
  fork again
    :执行节点 B;
  end fork
  :校验输出 Schema;
  if (执行失败?) then (是)
    :按 retry_policy 重试;
    if (仍失败?) then (是)
      :按 failure_policy 终止/跳过/默认值;
    endif
  endif
  :保存节点日志并推送状态;
endwhile (否)
:汇总输出并更新执行状态;
stop
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{工具流 DAG 校验与执行流程图}
\end{figure}

伪码：

\begin{lstlisting}
function run_workflow(workflow_id, input_payload, actor):
    workflow = workflow_repo.get_published(workflow_id)
    permission.ensure(actor, "workflow", workflow_id, "execute")

    graph = parse_graph(workflow.nodes, workflow.edges)
    validate_node_schema(graph.nodes)
    order = topological_sort(graph)
    if len(order) != len(graph.nodes):
        raise WorkflowDefinitionError("workflow graph contains cycle")

    execution = execution_repo.create(workflow_id, actor.id, input_payload)
    context = {"input": input_payload, "nodes": {}}

    ready_batches = build_parallel_batches(order, graph)
    for batch in ready_batches:
        results = parallel_run([
            lambda node: execute_node_with_retry(node, context)
            for node in batch
        ])
        for result in results:
            execution_repo.save_node_result(execution.id, result)
            websocket.publish(actor.id, "workflow.node.updated", result)

            if result.failed and result.failure_policy == "terminate":
                execution_repo.mark_failed(execution.id, result.error)
                return
            context["nodes"][result.node_id] = result.output

    output = aggregate_outputs(context)
    execution_repo.mark_success(execution.id, output)
    websocket.publish(actor.id, "workflow.completed", output)
\end{lstlisting}

\hypertarget{rbac-acl-ux6743ux9650ux5224ux5b9aux7b97ux6cd5}{%
\subsection{RBAC + ACL
权限判定算法}\label{rbac-acl-ux6743ux9650ux5224ux5b9aux7b97ux6cd5}}

\begin{lstlisting}
function can_access(user, resource_type, resource_id, action):
    if user.status != "active":
        return false

    subjects = [
        ("user", user.id),
        ("role", each role id of user),
        ("team", team ids where user is member)
    ]

    resource_chain = build_resource_chain(resource_type, resource_id)
    matched_rules = []
    for resource in resource_chain:
        matched_rules += permission_repo.find(subjects, resource, action)

    if any rule.effect == "deny" in matched_rules:
        return false
    if any rule.effect == "allow" in matched_rules:
        return true

    return default_policy(resource_type, action)
\end{lstlisting}

权限设计采用``显式拒绝优先、显式允许次之、默认策略兜底''的顺序。文件夹权限可继承到子文件夹和文件，单个资源可设置覆盖规则。

\hypertarget{ux9762ux5411ux5bf9ux8c61ux65b9ux6cd5ux7c7bux56feux8be6ux7ec6ux8bbeux8ba1}{%
\section{面向对象方法类图详细设计}\label{ux9762ux5411ux5bf9ux8c61ux65b9ux6cd5ux7c7bux56feux8be6ux7ec6ux8bbeux8ba1}}

\hypertarget{ux6838ux5fc3ux9886ux57dfux7c7bux56fe}{%
\subsection{核心领域类图}\label{ux6838ux5fc3ux9886ux57dfux7c7bux56fe}}

\begin{figure}[H]
\centering
\begin{adjustbox}{max width=\linewidth,max totalheight=0.78\textheight,center}
\begingroup
\renewcommand{\PlantUMLJobname}{plantuml-diagrams/req-uml-10}
\begin{plantuml}
@startuml
title 核心领域类图
hide circle
skinparam classAttributeIconSize 0

class User {
  +id: bigint
  +username: string
  +email: string
  +passwordHash: string
  +status: UserStatus
  +lastLoginAt: datetime
  +login()
  +updateProfile()
  +disable()
}

class Role {
  +id: bigint
  +name: string
  +scope: RoleScope
  +description: string
}

class PermissionRule {
  +id: bigint
  +subjectType: string
  +subjectId: bigint
  +resourceType: string
  +resourceId: bigint
  +action: string
  +effect: Effect
  +inherit: boolean
}

class Team {
  +id: bigint
  +name: string
  +description: string
  +createInvite()
  +addMember()
  +removeMember()
}

class TeamMember {
  +id: bigint
  +teamId: bigint
  +userId: bigint
  +role: TeamRole
  +status: MemberStatus
}

class Folder {
  +id: bigint
  +ownerId: bigint
  +teamId: bigint
  +parentId: bigint
  +name: string
  +path: string
  +scope: FolderScope
  +rename()
  +moveTo()
}

class File {
  +id: bigint
  +filename: string
  +mimeType: string
  +sizeBytes: long
  +sha256: string
  +storageKey: string
  +status: FileStatus
  +rename()
  +moveTo()
  +markDeleted()
}

class FileVersion {
  +id: bigint
  +versionNo: int
  +storageKey: string
  +sizeBytes: long
  +sha256: string
  +createdBy: bigint
}

class Tag {
  +id: bigint
  +name: string
  +color: string
  +source: TagSource
}

class KnowledgeBase {
  +id: bigint
  +ownerId: bigint
  +teamId: bigint
  +name: string
  +visibility: Visibility
  +status: KBStatus
  +archive()
}

class Document {
  +id: bigint
  +kbId: bigint
  +fileId: bigint
  +title: string
  +parseStatus: ParseStatus
  +contentText: text
  +chunkCount: int
}

class DocumentChunk {
  +id: bigint
  +documentId: bigint
  +chunkIndex: int
  +content: text
  +pageNo: int
  +paragraphNo: int
  +embeddingId: string
}

class Conversation {
  +id: bigint
  +userId: bigint
  +kbId: bigint
  +title: string
  +appendMessage()
}

class ConversationMessage {
  +id: bigint
  +conversationId: bigint
  +role: MessageRole
  +content: text
  +citations: json
}

class ToolDefinition {
  +id: bigint
  +name: string
  +version: string
  +inputSchema: json
  +outputSchema: json
  +invokeType: InvokeType
  +validateInput()
  +testInvoke()
}

class WorkflowDefinition {
  +id: bigint
  +ownerId: bigint
  +teamId: bigint
  +name: string
  +nodes: json
  +edges: json
  +version: int
  +validateDAG()
  +publish()
}

class WorkflowExecution {
  +id: bigint
  +workflowId: bigint
  +startedBy: bigint
  +status: ExecutionStatus
  +start()
  +cancel()
  +markSuccess()
  +markFailed()
}

class WorkflowNodeExecution {
  +id: bigint
  +executionId: bigint
  +nodeId: string
  +toolId: bigint
  +status: ExecutionStatus
  +inputPayload: json
  +outputPayload: json
  +retryCount: int
}

class ChatMessage {
  +id: bigint
  +teamId: bigint
  +senderId: bigint
  +receiverId: bigint
  +content: text
  +messageType: string
}

class FileAnnotation {
  +id: bigint
  +fileId: bigint
  +authorId: bigint
  +parentId: bigint
  +content: text
  +position: json
}

User "1" -- "0..*" Folder
User "1" -- "0..*" File
User "0..*" -- "0..*" Role
Role "1" -- "0..*" PermissionRule
Team "1" -- "0..*" TeamMember
User "1" -- "0..*" TeamMember
Folder "1" -- "0..*" Folder
Folder "1" -- "0..*" File
File "1" -- "1..*" FileVersion
File "0..*" -- "0..*" Tag
KnowledgeBase "1" -- "0..*" Document
File "1" -- "0..1" Document
Document "1" -- "1..*" DocumentChunk
Conversation "1" -- "0..*" ConversationMessage
WorkflowDefinition "1" -- "0..*" WorkflowExecution
WorkflowExecution "1" -- "0..*" WorkflowNodeExecution
WorkflowDefinition "0..*" -- "0..*" ToolDefinition
Team "1" -- "0..*" ChatMessage
File "1" -- "0..*" FileAnnotation
@enduml
\end{plantuml}
\endgroup
\end{adjustbox}
\caption{核心领域类图}
\end{figure}

\hypertarget{ux670dux52a1ux7c7bux8bbeux8ba1}{%
\subsection{服务类设计}\label{ux670dux52a1ux7c7bux8bbeux8ba1}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
服务类 & 主要方法 & 说明\tabularnewline
\hline
\endhead
AuthService & register()、login()、refresh\_token()、hash\_password() & 用户注册、登录和 Token 管理\tabularnewline
\hline
PermissionService & can\_access()、ensure()、grant()、revoke() & RBAC/ACL 权限判定\tabularnewline
\hline
FileService & upload()、download\_url()、move()、rename()、create\_version() & 文件生命周期管理\tabularnewline
\hline
DocumentParseService & parse\_file()、extract\_text()、extract\_ocr() & 文档解析与 OCR\tabularnewline
\hline
EmbeddingService & embed\_text()、index\_chunks() & 分块向量化与索引写入\tabularnewline
\hline
RAGService & retrieve()、rerank()、answer() & 检索增强生成\tabularnewline
\hline
ToolRegistryService & register\_tool()、validate\_input()、invoke() & 工具注册与调用\tabularnewline
\hline
AgentService & run\_task()、run\_react\_loop() & 智能体任务编排\tabularnewline
\hline
WorkflowService & validate\_dag()、start\_execution()、run\_node() & 工具流校验与执行\tabularnewline
\hline
TeamService & create\_team()、invite()、set\_role() & 团队与成员管理\tabularnewline
\hline
NotificationService & push()、mark\_read() & 通知与 WebSocket 推送\tabularnewline
\hline
AuditService & record()、query\_logs() & 审计记录与查询\tabularnewline
\hline
\end{longtable}

\hypertarget{ux63a5ux53e3ux8bbeux8ba1}{%
\section{接口设计}\label{ux63a5ux53e3ux8bbeux8ba1}}

\hypertarget{ux63a5ux53e3ux89c4ux8303}{%
\subsection{接口规范}\label{ux63a5ux53e3ux89c4ux8303}}

\begin{itemize}
\tightlist
\item
  基础路径：\passthrough{\lstinline!/api/v1!}
\item
  认证方式：\passthrough{\lstinline!Authorization: Bearer <access\_token>!}
\item
  请求/响应格式：JSON；文件上传使用
  \passthrough{\lstinline!multipart/form-data!}
\item
  时间格式：ISO 8601
\item
  分页参数：\passthrough{\lstinline!page!}、\passthrough{\lstinline!page\_size!}
\item
  统一错误响应：
\end{itemize}

\begin{lstlisting}
{
    "code": "PERMISSION_DENIED",
    "message": "无权访问该资源",
    "detail": {
        "resource_type": "file",
        "resource_id": 1001
    }
}
\end{lstlisting}

\hypertarget{ux7528ux6237ux8ba4ux8bc1ux63a5ux53e3}{%
\subsection{用户认证接口}\label{ux7528ux6237ux8ba4ux8bc1ux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
POST & /auth/register & 用户注册\tabularnewline
\hline
POST & /auth/login & 用户登录，返回 Access Token 和 Refresh Token\tabularnewline
\hline
POST & /auth/refresh & 刷新 Access Token\tabularnewline
\hline
GET & /users/me & 获取当前用户信息\tabularnewline
\hline
PATCH & /users/me & 修改个人资料\tabularnewline
\hline
\end{longtable}

登录响应示例：

\begin{lstlisting}
{
    "access_token": "jwt-access-token",
    "refresh_token": "jwt-refresh-token",
    "token_type": "bearer",
    "expires_in": 1800,
    "user": {
        "id": 1,
        "username": "xiaoming",
        "email": "xiaoming@example.com"
    }
}
\end{lstlisting}

\hypertarget{ux6587ux4ef6ux7ba1ux7406ux63a5ux53e3}{%
\subsection{文件管理接口}\label{ux6587ux4ef6ux7ba1ux7406ux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
GET & /folders/tree & 获取个人或团队文件夹树\tabularnewline
\hline
POST & /folders & 创建文件夹\tabularnewline
\hline
PATCH & /folders/\{folder\_id\} & 重命名或移动文件夹\tabularnewline
\hline
DELETE & /folders/\{folder\_id\} & 删除文件夹\tabularnewline
\hline
GET & /files & 文件列表、搜索和筛选\tabularnewline
\hline
POST & /files/upload & 上传文件\tabularnewline
\hline
POST & /files/chunk/init & 初始化分片上传\tabularnewline
\hline
PUT & /files/chunk/\{upload\_id\}/\{part\_no\} & 上传分片\tabularnewline
\hline
POST & /files/chunk/\{upload\_id\}/complete & 合并分片\tabularnewline
\hline
GET & /files/\{file\_id\}/download-url & 获取短期下载链接\tabularnewline
\hline
PATCH & /files/\{file\_id\} & 重命名、移动或修改标签\tabularnewline
\hline
DELETE & /files/\{file\_id\} & 软删除文件\tabularnewline
\hline
GET & /files/\{file\_id\}/versions & 查询文件版本\tabularnewline
\hline
POST & /files/\{file\_id\}/versions/\{version\_id\}/restore & 回滚版本\tabularnewline
\hline
POST & /files/\{file\_id\}/share-links & 创建分享链接\tabularnewline
\hline
\end{longtable}

\hypertarget{ux77e5ux8bc6ux5e93ux4e0e-rag-ux63a5ux53e3}{%
\subsection{知识库与 RAG
接口}\label{ux77e5ux8bc6ux5e93ux4e0e-rag-ux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
GET & /knowledge-bases & 查询知识库\tabularnewline
\hline
POST & /knowledge-bases & 创建知识库\tabularnewline
\hline
PATCH & /knowledge-bases/\{kb\_id\} & 修改知识库\tabularnewline
\hline
POST & /knowledge-bases/\{kb\_id\}/documents & 将文件加入知识库\tabularnewline
\hline
GET & /knowledge-bases/\{kb\_id\}/documents & 查询知识库文档\tabularnewline
\hline
POST & /documents/\{document\_id\}/reparse & 重新解析文档\tabularnewline
\hline
POST & /qa/query & 发起知识库问答\tabularnewline
\hline
GET & /conversations & 查询会话列表\tabularnewline
\hline
GET & /conversations/\{conversation\_id\}/messages & 查询会话消息\tabularnewline
\hline
\end{longtable}

问答请求示例：

\begin{lstlisting}
{
    "kb_id": 10,
    "conversation_id": 21,
    "question": "总结所有用到显微镜的实验步骤",
    "top_k": 8,
    "stream": true
}
\end{lstlisting}

问答响应事件示例：

\begin{lstlisting}
{
    "event": "answer.delta",
    "content": "显微镜相关实验步骤包括..."
}
\end{lstlisting}

\begin{lstlisting}
{
    "event": "answer.completed",
    "message_id": 301,
    "citations": [
        {
            "file_id": 1001,
            "document_id": 201,
            "chunk_id": 9001,
            "title": "生物实验报告.pdf",
            "page_no": 3,
            "paragraph_no": 5
        }
    ]
}
\end{lstlisting}

\hypertarget{agent-ux4e0eux5de5ux5177ux63a5ux53e3}{%
\subsection{Agent
与工具接口}\label{agent-ux4e0eux5de5ux5177ux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
GET & /tools & 查询可用工具\tabularnewline
\hline
POST & /admin/tools & 管理员注册工具\tabularnewline
\hline
POST & /admin/tools/\{tool\_id\}/test & 工具测试调用\tabularnewline
\hline
PATCH & /admin/tools/\{tool\_id\} & 修改工具定义或状态\tabularnewline
\hline
POST & /agents/tasks & 创建智能体任务\tabularnewline
\hline
GET & /agents/tasks/\{task\_id\} & 查询任务状态和执行步骤\tabularnewline
\hline
POST & /agents/tasks/\{task\_id\}/cancel & 取消任务\tabularnewline
\hline
\end{longtable}

工具定义示例：

\begin{lstlisting}
{
    "name": "knowledge_qa",
    "version": "1.0.0",
    "category": "AI处理",
    "input_schema": {
        "type": "object",
        "required": ["kb_id", "question"],
        "properties": {
            "kb_id": { "type": "integer" },
            "question": { "type": "string" }
        }
    },
    "output_schema": {
        "type": "object",
        "properties": {
            "answer": { "type": "string" },
            "citations": { "type": "array" }
        }
    },
    "invoke_type": "python",
    "invoke_config": {
        "module": "app.tools.knowledge_qa",
        "function": "run"
    }
}
\end{lstlisting}

\hypertarget{ux5de5ux5177ux6d41ux63a5ux53e3}{%
\subsection{工具流接口}\label{ux5de5ux5177ux6d41ux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
GET & /workflows & 查询流程定义\tabularnewline
\hline
POST & /workflows & 创建流程\tabularnewline
\hline
PATCH & /workflows/\{workflow\_id\} & 修改流程\tabularnewline
\hline
POST & /workflows/\{workflow\_id\}/validate & 校验 DAG 和参数\tabularnewline
\hline
POST & /workflows/\{workflow\_id\}/publish & 发布流程\tabularnewline
\hline
POST & /workflows/\{workflow\_id\}/executions & 执行流程\tabularnewline
\hline
GET & /workflow-executions/\{execution\_id\} & 查询执行详情\tabularnewline
\hline
POST & /workflow-executions/\{execution\_id\}/cancel & 取消执行\tabularnewline
\hline
\end{longtable}

WebSocket 事件：

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-4\tabcolsep-3\arrayrulewidth)/1\relax}|}
\hline
事件 & 说明\tabularnewline
\hline
\endhead
workflow.started & 流程开始\tabularnewline
\hline
workflow.node.running & 节点开始执行\tabularnewline
\hline
workflow.node.success & 节点成功\tabularnewline
\hline
workflow.node.failed & 节点失败\tabularnewline
\hline
workflow.completed & 流程完成\tabularnewline
\hline
workflow.failed & 流程失败\tabularnewline
\hline
\end{longtable}

\hypertarget{ux56e2ux961fux534fux4f5cux63a5ux53e3}{%
\subsection{团队协作接口}\label{ux56e2ux961fux534fux4f5cux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
POST & /teams & 创建团队\tabularnewline
\hline
GET & /teams & 查询我的团队\tabularnewline
\hline
POST & /teams/\{team\_id\}/invites & 创建邀请\tabularnewline
\hline
POST & /teams/\{team\_id\}/members & 加入或添加成员\tabularnewline
\hline
PATCH & /teams/\{team\_id\}/members/\{member\_id\} & 修改成员角色\tabularnewline
\hline
DELETE & /teams/\{team\_id\}/members/\{member\_id\} & 移除成员\tabularnewline
\hline
GET & /teams/\{team\_id\}/messages & 查询聊天历史\tabularnewline
\hline
POST & /teams/\{team\_id\}/messages & 发送消息\tabularnewline
\hline
GET & /files/\{file\_id\}/annotations & 查询文件批注\tabularnewline
\hline
POST & /files/\{file\_id\}/annotations & 创建批注\tabularnewline
\hline
POST & /annotations/\{annotation\_id\}/replies & 回复批注\tabularnewline
\hline
GET & /notifications & 查询通知\tabularnewline
\hline
PATCH & /notifications/\{notification\_id\}/read & 标记已读\tabularnewline
\hline
\end{longtable}

\hypertarget{ux6743ux9650ux4e0eux5ba1ux8ba1ux63a5ux53e3}{%
\subsection{权限与审计接口}\label{ux6743ux9650ux4e0eux5ba1ux8ba1ux63a5ux53e3}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-6\tabcolsep-4\arrayrulewidth)/2\relax}|}
\hline
方法 & 路径 & 说明\tabularnewline
\hline
\endhead
GET & /permissions/rules & 查询权限规则\tabularnewline
\hline
POST & /permissions/rules & 创建授权规则\tabularnewline
\hline
DELETE & /permissions/rules/\{rule\_id\} & 删除授权规则\tabularnewline
\hline
GET & /audit-logs & 查询审计日志\tabularnewline
\hline
\end{longtable}

\hypertarget{ux6570ux636eux5e93ux7269ux7406ux8bbeux8ba1}{%
\section{数据库物理设计}\label{ux6570ux636eux5e93ux7269ux7406ux8bbeux8ba1}}

\hypertarget{ux8bbeux8ba1ux7ea6ux5b9a}{%
\subsection{设计约定}\label{ux8bbeux8ba1ux7ea6ux5b9a}}

\begin{itemize}
\tightlist
\item
  数据库：MySQL 8.0
\item
  字符集：\passthrough{\lstinline!utf8mb4!}
\item
  排序规则：\passthrough{\lstinline!utf8mb4\_0900\_ai\_ci!}
\item
  主键：\passthrough{\lstinline!BIGINT UNSIGNED AUTO\_INCREMENT!}
\item
  时间字段：\passthrough{\lstinline!DATETIME(3)!}，统一存储服务器时间或
  UTC
\item
  JSON 字段：使用 MySQL \passthrough{\lstinline!JSON!} 类型保存
  Schema、流程节点、引用来源和执行输入输出
\item
  删除策略：业务数据优先软删除，审计日志只追加不更新
\end{itemize}

\hypertarget{ux6838ux5fc3ux7269ux7406ux8868}{%
\subsection{核心物理表}\label{ux6838ux5fc3ux7269ux7406ux8868}}

\hypertarget{ux7528ux6237ux4e0eux6743ux9650ux57df}{%
\subsubsection{用户与权限域}\label{ux7528ux6237ux4e0eux6743ux9650ux57df}}

\begin{lstlisting}[language=SQL]
CREATE TABLE users (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(64) NOT NULL UNIQUE,
  email VARCHAR(128) NOT NULL UNIQUE,
  password_hash VARCHAR(255) NOT NULL,
  role_type VARCHAR(32) NOT NULL DEFAULT 'user',
  status VARCHAR(32) NOT NULL DEFAULT 'active',
  last_login_at DATETIME(3) NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  INDEX idx_users_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE roles (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(64) NOT NULL,
  scope VARCHAR(32) NOT NULL,
  description VARCHAR(255) NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_roles_name_scope (name, scope)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE user_roles (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT UNSIGNED NOT NULL,
  role_id BIGINT UNSIGNED NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_user_roles (user_id, role_id),
  CONSTRAINT fk_user_roles_user FOREIGN KEY (user_id) REFERENCES users(id),
  CONSTRAINT fk_user_roles_role FOREIGN KEY (role_id) REFERENCES roles(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE permission_rules (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  subject_type VARCHAR(32) NOT NULL,
  subject_id BIGINT UNSIGNED NOT NULL,
  resource_type VARCHAR(32) NOT NULL,
  resource_id BIGINT UNSIGNED NOT NULL,
  action VARCHAR(32) NOT NULL,
  effect VARCHAR(16) NOT NULL,
  inherit BOOLEAN NOT NULL DEFAULT TRUE,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  INDEX idx_perm_subject (subject_type, subject_id),
  INDEX idx_perm_resource (resource_type, resource_id),
  INDEX idx_perm_action (action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
\end{lstlisting}

\hypertarget{ux6587ux4ef6ux7ba1ux7406ux57df}{%
\subsubsection{文件管理域}\label{ux6587ux4ef6ux7ba1ux7406ux57df}}

\begin{lstlisting}[language=SQL]
CREATE TABLE folders (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  owner_id BIGINT UNSIGNED NULL,
  team_id BIGINT UNSIGNED NULL,
  parent_id BIGINT UNSIGNED NULL,
  name VARCHAR(128) NOT NULL,
  path VARCHAR(1024) NOT NULL,
  scope VARCHAR(32) NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  INDEX idx_folders_owner_parent (owner_id, parent_id),
  INDEX idx_folders_team_parent (team_id, parent_id),
  CONSTRAINT fk_folders_parent FOREIGN KEY (parent_id) REFERENCES folders(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE files (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  owner_id BIGINT UNSIGNED NULL,
  team_id BIGINT UNSIGNED NULL,
  folder_id BIGINT UNSIGNED NOT NULL,
  filename VARCHAR(255) NOT NULL,
  extension VARCHAR(32) NULL,
  mime_type VARCHAR(128) NOT NULL,
  size_bytes BIGINT UNSIGNED NOT NULL,
  sha256 CHAR(64) NOT NULL,
  storage_key VARCHAR(512) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'active',
  sensitive BOOLEAN NOT NULL DEFAULT FALSE,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  FULLTEXT KEY ft_files_filename (filename),
  INDEX idx_files_folder (folder_id),
  INDEX idx_files_owner_status (owner_id, status),
  INDEX idx_files_team_status (team_id, status),
  INDEX idx_files_sha256 (sha256),
  CONSTRAINT fk_files_folder FOREIGN KEY (folder_id) REFERENCES folders(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE file_versions (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  file_id BIGINT UNSIGNED NOT NULL,
  version_no INT NOT NULL,
  storage_key VARCHAR(512) NOT NULL,
  size_bytes BIGINT UNSIGNED NOT NULL,
  sha256 CHAR(64) NOT NULL,
  created_by BIGINT UNSIGNED NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_file_versions (file_id, version_no),
  CONSTRAINT fk_file_versions_file FOREIGN KEY (file_id) REFERENCES files(id),
  CONSTRAINT fk_file_versions_user FOREIGN KEY (created_by) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE tags (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  owner_id BIGINT UNSIGNED NULL,
  name VARCHAR(64) NOT NULL,
  color VARCHAR(16) NULL,
  source VARCHAR(32) NOT NULL DEFAULT 'manual',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_tags_owner_name (owner_id, name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE file_tags (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  file_id BIGINT UNSIGNED NOT NULL,
  tag_id BIGINT UNSIGNED NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_file_tags (file_id, tag_id),
  CONSTRAINT fk_file_tags_file FOREIGN KEY (file_id) REFERENCES files(id),
  CONSTRAINT fk_file_tags_tag FOREIGN KEY (tag_id) REFERENCES tags(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
\end{lstlisting}

\hypertarget{ux77e5ux8bc6ux5e93ux4e0eux95eeux7b54ux57df}{%
\subsubsection{知识库与问答域}\label{ux77e5ux8bc6ux5e93ux4e0eux95eeux7b54ux57df}}

\begin{lstlisting}[language=SQL]
CREATE TABLE knowledge_bases (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  owner_id BIGINT UNSIGNED NOT NULL,
  team_id BIGINT UNSIGNED NULL,
  name VARCHAR(128) NOT NULL,
  description TEXT NULL,
  visibility VARCHAR(32) NOT NULL DEFAULT 'private',
  status VARCHAR(32) NOT NULL DEFAULT 'active',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  INDEX idx_kb_owner (owner_id),
  INDEX idx_kb_team (team_id),
  CONSTRAINT fk_kb_owner FOREIGN KEY (owner_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE documents (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  kb_id BIGINT UNSIGNED NOT NULL,
  file_id BIGINT UNSIGNED NOT NULL,
  title VARCHAR(255) NOT NULL,
  parse_status VARCHAR(32) NOT NULL DEFAULT 'pending',
  content_text LONGTEXT NULL,
  chunk_count INT NOT NULL DEFAULT 0,
  parser_version VARCHAR(32) NULL,
  parsed_at DATETIME(3) NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  INDEX idx_documents_kb (kb_id),
  INDEX idx_documents_file (file_id),
  INDEX idx_documents_status (parse_status),
  CONSTRAINT fk_documents_kb FOREIGN KEY (kb_id) REFERENCES knowledge_bases(id),
  CONSTRAINT fk_documents_file FOREIGN KEY (file_id) REFERENCES files(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE document_chunks (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  document_id BIGINT UNSIGNED NOT NULL,
  chunk_index INT NOT NULL,
  content TEXT NOT NULL,
  page_no INT NULL,
  paragraph_no INT NULL,
  start_offset INT NULL,
  end_offset INT NULL,
  embedding_id VARCHAR(128) NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_document_chunk (document_id, chunk_index),
  INDEX idx_chunks_embedding (embedding_id),
  CONSTRAINT fk_chunks_document FOREIGN KEY (document_id) REFERENCES documents(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE conversations (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT UNSIGNED NOT NULL,
  kb_id BIGINT UNSIGNED NULL,
  title VARCHAR(255) NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  INDEX idx_conversations_user (user_id),
  INDEX idx_conversations_kb (kb_id),
  CONSTRAINT fk_conversations_user FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE conversation_messages (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  conversation_id BIGINT UNSIGNED NOT NULL,
  role VARCHAR(32) NOT NULL,
  content LONGTEXT NOT NULL,
  citations JSON NULL,
  token_count INT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  INDEX idx_messages_conversation_time (conversation_id, created_at),
  CONSTRAINT fk_messages_conversation FOREIGN KEY (conversation_id) REFERENCES conversations(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
\end{lstlisting}

\hypertarget{ux5de5ux5177ux6d41ux4e0eux56e2ux961fux534fux4f5cux57df}{%
\subsubsection{工具流与团队协作域}\label{ux5de5ux5177ux6d41ux4e0eux56e2ux961fux534fux4f5cux57df}}

\begin{lstlisting}[language=SQL]
CREATE TABLE tool_definitions (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(128) NOT NULL,
  version VARCHAR(32) NOT NULL,
  category VARCHAR(64) NOT NULL,
  description TEXT NULL,
  input_schema JSON NOT NULL,
  output_schema JSON NOT NULL,
  invoke_type VARCHAR(32) NOT NULL,
  invoke_config JSON NOT NULL,
  timeout_seconds INT NOT NULL DEFAULT 30,
  status VARCHAR(32) NOT NULL DEFAULT 'draft',
  created_by BIGINT UNSIGNED NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_tool_name_version (name, version),
  INDEX idx_tools_status (status),
  CONSTRAINT fk_tools_creator FOREIGN KEY (created_by) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE workflow_definitions (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  owner_id BIGINT UNSIGNED NOT NULL,
  team_id BIGINT UNSIGNED NULL,
  name VARCHAR(128) NOT NULL,
  description TEXT NULL,
  nodes JSON NOT NULL,
  edges JSON NOT NULL,
  trigger_type VARCHAR(32) NOT NULL DEFAULT 'manual',
  version INT NOT NULL DEFAULT 1,
  status VARCHAR(32) NOT NULL DEFAULT 'draft',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  INDEX idx_workflows_owner (owner_id),
  INDEX idx_workflows_team (team_id),
  CONSTRAINT fk_workflows_owner FOREIGN KEY (owner_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE workflow_executions (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  workflow_id BIGINT UNSIGNED NOT NULL,
  started_by BIGINT UNSIGNED NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  input_payload JSON NULL,
  output_payload JSON NULL,
  error_message TEXT NULL,
  started_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  ended_at DATETIME(3) NULL,
  INDEX idx_exec_workflow (workflow_id),
  INDEX idx_exec_user_status (started_by, status),
  CONSTRAINT fk_exec_workflow FOREIGN KEY (workflow_id) REFERENCES workflow_definitions(id),
  CONSTRAINT fk_exec_user FOREIGN KEY (started_by) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE workflow_node_executions (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  execution_id BIGINT UNSIGNED NOT NULL,
  node_id VARCHAR(64) NOT NULL,
  tool_id BIGINT UNSIGNED NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  input_payload JSON NULL,
  output_payload JSON NULL,
  retry_count INT NOT NULL DEFAULT 0,
  error_message TEXT NULL,
  started_at DATETIME(3) NULL,
  ended_at DATETIME(3) NULL,
  INDEX idx_node_exec (execution_id, node_id),
  CONSTRAINT fk_node_exec_execution FOREIGN KEY (execution_id) REFERENCES workflow_executions(id),
  CONSTRAINT fk_node_exec_tool FOREIGN KEY (tool_id) REFERENCES tool_definitions(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE teams (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(128) NOT NULL,
  description TEXT NULL,
  created_by BIGINT UNSIGNED NOT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  CONSTRAINT fk_teams_creator FOREIGN KEY (created_by) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE team_members (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  team_id BIGINT UNSIGNED NOT NULL,
  user_id BIGINT UNSIGNED NOT NULL,
  role VARCHAR(32) NOT NULL DEFAULT 'member',
  status VARCHAR(32) NOT NULL DEFAULT 'active',
  joined_at DATETIME(3) NULL,
  UNIQUE KEY uk_team_member (team_id, user_id),
  CONSTRAINT fk_team_members_team FOREIGN KEY (team_id) REFERENCES teams(id),
  CONSTRAINT fk_team_members_user FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE chat_messages (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  team_id BIGINT UNSIGNED NOT NULL,
  sender_id BIGINT UNSIGNED NOT NULL,
  receiver_id BIGINT UNSIGNED NULL,
  content TEXT NOT NULL,
  message_type VARCHAR(32) NOT NULL DEFAULT 'text',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  INDEX idx_chat_team_time (team_id, created_at),
  CONSTRAINT fk_chat_team FOREIGN KEY (team_id) REFERENCES teams(id),
  CONSTRAINT fk_chat_sender FOREIGN KEY (sender_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
\end{lstlisting}

\hypertarget{ux5411ux91cfux7d22ux5f15ux7269ux7406ux8bbeux8ba1}{%
\subsection{向量索引物理设计}\label{ux5411ux91cfux7d22ux5f15ux7269ux7406ux8bbeux8ba1}}

FAISS/Milvus 中每个向量保存以下 metadata：

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-4\tabcolsep-3\arrayrulewidth)/1\relax}|}
\hline
字段 & 说明\tabularnewline
\hline
\endhead
embedding\_id & 向量唯一编号，对应 document\_chunks.embedding\_id\tabularnewline
\hline
kb\_id & 知识库编号\tabularnewline
\hline
file\_id & 文件编号\tabularnewline
\hline
document\_id & 文档编号\tabularnewline
\hline
chunk\_id & 分块编号\tabularnewline
\hline
chunk\_index & 分块序号\tabularnewline
\hline
page\_no & 页码\tabularnewline
\hline
paragraph\_no & 段落号\tabularnewline
\hline
visibility\_scope & private/team/public\tabularnewline
\hline
\end{longtable}

FAISS 本地部署时，索引文件建议按知识库拆分：

\begin{lstlisting}
storage/vector_indexes/
+-- kb_10.index
+-- kb_10.meta.json
+-- kb_11.index
+-- kb_11.meta.json
\end{lstlisting}

\hypertarget{ui-ux754cux9762ux8bbeux8ba1}{%
\section{UI 界面设计}\label{ui-ux754cux9762ux8bbeux8ba1}}

\hypertarget{ux8bbeux8ba1ux539fux5219}{%
\subsection{设计原则}\label{ux8bbeux8ba1ux539fux5219}}

界面遵循根目录 \passthrough{\lstinline!DESIGN.md!}
的项目设计系统：安静、学术、操作型、信息密度适中。系统首屏应是可工作的知识工作台，不设计营销型首页。颜色以
\passthrough{\lstinline!\#F6F8FB!}
背景、白色操作面板、\passthrough{\lstinline!\#246BFE!}
主色、绿色/黄色/红色状态色和少量紫色 AI 标识构成。

\hypertarget{ux6574ux4f53ux5e03ux5c40}{%
\subsection{整体布局}\label{ux6574ux4f53ux5e03ux5c40}}

\begin{lstlisting}
+--------------------------------------------------------------+
| 顶部栏：当前空间 / 搜索 / 通知 / 用户菜单                    |
+---------------+------------------------------+---------------+
| 左侧导航      | 主工作区                     | 右侧上下文面板 |
| - 个人文件    | - 文件表格/知识库/流程画布   | - RAG 问答    |
| - 团队空间    | - 批量操作工具条             | - 引用来源    |
| - 知识库      | - 状态/筛选/分页             | - 执行步骤    |
| - 工具流      |                              | - 文件详情    |
| - 管理后台    |                              |               |
+---------------+------------------------------+---------------+
\end{lstlisting}

\hypertarget{ux4e3bux8981ux9875ux9762ux8bbeux8ba1}{%
\subsection{主要页面设计}\label{ux4e3bux8981ux9875ux9762ux8bbeux8ba1}}

\hypertarget{ux6587ux4ef6ux7ba1ux7406ux5de5ux4f5cux53f0}{%
\subsubsection{文件管理工作台}\label{ux6587ux4ef6ux7ba1ux7406ux5de5ux4f5cux53f0}}

\begin{itemize}
\tightlist
\item
  左侧：个人空间/团队空间文件夹树。
\item
  顶部：面包屑、上传按钮、新建文件夹、搜索框、类型/标签/时间筛选。
\item
  中间：文件表格，列包括文件名、类型、大小、解析状态、标签、更新时间、权限范围。
\item
  右侧：文件详情抽屉，展示元数据、版本、分享链接、解析状态和关联知识库。
\item
  状态：\passthrough{\lstinline!解析中!}、\passthrough{\lstinline!已入库!}、\passthrough{\lstinline!解析失败!}、\passthrough{\lstinline!无权限!}
  使用稳定宽度状态标签。
\end{itemize}

\hypertarget{rag-ux77e5ux8bc6ux95eeux7b54ux754cux9762}{%
\subsubsection{RAG
知识问答界面}\label{rag-ux77e5ux8bc6ux95eeux7b54ux754cux9762}}

\begin{itemize}
\tightlist
\item
  左侧：知识库列表和文档范围选择。
\item
  中间：对话流，答案采用流式显示。
\item
  右侧：引用来源列表，显示文档名、页码、段落和可信片段预览。
\item
  交互：点击引用跳转文件预览对应页码；无权限引用不展示正文，只显示权限提示。
\end{itemize}

\hypertarget{ux5de5ux5177ux6d41ux7f16ux6392ux9875ux9762}{%
\subsubsection{工具流编排页面}\label{ux5de5ux5177ux6d41ux7f16ux6392ux9875ux9762}}

\begin{itemize}
\tightlist
\item
  中间大区域：Vue Flow
  画布，显示工具节点、条件节点、聚合节点、输出节点。
\item
  左侧：工具市场面板，按文件操作、AI 处理、数据分析、通知类分组。
\item
  右侧：节点配置抽屉，显示参数 Schema 表单、上游输出绑定和失败策略。
\item
  底部：执行历史和节点日志表格。
\item
  WebSocket
  状态：节点边框和状态徽标实时更新，失败节点可查看错误详情并重试。
\end{itemize}

\hypertarget{agent-ux6267ux884cux9762ux677f}{%
\subsubsection{Agent 执行面板}\label{agent-ux6267ux884cux9762ux677f}}

\begin{itemize}
\tightlist
\item
  输入区：自然语言任务输入与上下文文件/知识库选择。
\item
  执行区：按时间线展示 Thought 摘要、Action 工具名、Observation
  结果摘要。
\item
  结果区：最终答案、报告或导出文件。
\item
  审计信息：展示任务 ID、发起人、执行耗时、工具调用次数。
\end{itemize}

\hypertarget{ux56e2ux961fux534fux4f5cux9875ux9762}{%
\subsubsection{团队协作页面}\label{ux56e2ux961fux534fux4f5cux9875ux9762}}

\begin{itemize}
\tightlist
\item
  左侧：团队列表、成员和角色。
\item
  中间：团队文件夹与活动动态。
\item
  右侧：团队聊天、通知、文件批注。
\item
  成员管理：团队管理员可邀请、移除成员和修改角色；普通成员只可查看成员列表。
\end{itemize}

\hypertarget{ux7ba1ux7406ux540eux53f0}{%
\subsubsection{管理后台}\label{ux7ba1ux7406ux540eux53f0}}

\begin{itemize}
\tightlist
\item
  用户管理：用户列表、状态、角色分配。
\item
  工具管理：工具 Schema 编辑、测试调用、版本和权限配置。
\item
  权限管理：资源 ACL、继承状态和覆盖规则。
\item
  审计日志：按用户、资源、动作、时间过滤。
\end{itemize}

\hypertarget{ux54cdux5e94ux5f0fux8bbeux8ba1}{%
\subsection{响应式设计}\label{ux54cdux5e94ux5f0fux8bbeux8ba1}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-4\tabcolsep-3\arrayrulewidth)/1\relax}|}
\hline
视口 & 设计\tabularnewline
\hline
\endhead
桌面端 >=1200px & 左侧导航 + 主工作区 + 右侧上下文面板三栏布局\tabularnewline
\hline
平板 768px-1199px & 左侧导航收起为图标栏，右侧面板改为覆盖式抽屉\tabularnewline
\hline
手机 <768px & 单列布局，文件表格变为分组列表，筛选和详情使用全屏抽屉\tabularnewline
\hline
\end{longtable}

\hypertarget{ux53efux8bbfux95eeux6027ux548cux5b89ux5168ux6e32ux67d3}{%
\subsection{可访问性和安全渲染}\label{ux53efux8bbfux95eeux6027ux548cux5b89ux5168ux6e32ux67d3}}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  所有图标按钮必须提供 tooltip 或 \passthrough{\lstinline!aria-label!}。
\item
  文件名、聊天消息、批注、工具描述等用户输入在渲染时必须转义。
\item
  主操作按钮有明确可见焦点态。
\item
  状态变化通过文本和颜色共同表达，避免仅依赖颜色。
\item
  长文件名、长标签、长错误消息必须省略或换行，不撑破布局。
\end{enumerate}

\hypertarget{ux975eux529fux80fdux8bbeux8ba1}{%
\section{非功能设计}\label{ux975eux529fux80fdux8bbeux8ba1}}

\hypertarget{ux6027ux80fdux8bbeux8ba1}{%
\subsection{性能设计}\label{ux6027ux80fdux8bbeux8ba1}}

\begin{itemize}
\tightlist
\item
  文件列表、搜索、权限校验等普通 API 目标平均响应时间不超过 500ms。
\item
  上传完成后异步解析，避免 OCR 和 Embedding 阻塞上传请求。
\item
  RAG 问答采用流式输出，首字响应时间目标不超过 5s。
\item
  WebSocket 消息端到端延迟目标不超过 500ms。
\item
  工具默认超时时间 30s，超时后按节点策略重试或降级。
\end{itemize}

\hypertarget{ux5b89ux5168ux8bbeux8ba1}{%
\subsection{安全设计}\label{ux5b89ux5168ux8bbeux8ba1}}

\begin{itemize}
\tightlist
\item
  密码使用 bcrypt 哈希，禁止明文存储。
\item
  所有受保护 API 和 WebSocket 建连必须校验 JWT。
\item
  文件下载使用短期签名 URL，有效期默认不超过 1 小时。
\item
  上传校验扩展名、MIME、大小、SHA256 和路径穿越风险。
\item
  用户输入统一转义，Markdown 渲染禁用危险 HTML 或使用白名单清洗。
\item
  管理员操作、权限变更、文件删除、分享创建和工具执行写入审计日志。
\end{itemize}

\hypertarget{ux53efux9760ux6027ux8bbeux8ba1}{%
\subsection{可靠性设计}\label{ux53efux9760ux6027ux8bbeux8ba1}}

\begin{itemize}
\tightlist
\item
  文档解析失败不影响文件基础访问，失败原因可见。
\item
  LLM 不可用时返回降级提示，已完成索引和文件管理功能继续可用。
\item
  WebSocket 支持断线重连，重连后拉取最新通知和流程状态。
\item
  数据库和对象存储定期备份，备份失败写入告警日志。
\end{itemize}

\hypertarget{ux90e8ux7f72ux8bbeux8ba1}{%
\section{部署设计}\label{ux90e8ux7f72ux8bbeux8ba1}}

\hypertarget{ux5f00ux53d1ux73afux5883}{%
\subsection{开发环境}\label{ux5f00ux53d1ux73afux5883}}

\begin{lstlisting}
frontend: Vue 3 + Vite + pnpm
backend: Python 3.13 + uv + FastAPI
database: MySQL 8.0
object storage: MinIO 或本地目录
vector store: FAISS 本地索引
queue: Redis + ARQ/Celery
\end{lstlisting}

\hypertarget{ux751fux4ea7ux90e8ux7f72}{%
\subsection{生产部署}\label{ux751fux4ea7ux90e8ux7f72}}

\begin{lstlisting}
Browser
  -> Nginx HTTPS
    -> frontend static assets
    -> FastAPI Uvicorn/Gunicorn workers
      -> MySQL
      -> Redis
      -> MinIO
      -> FAISS local volume or Milvus service
      -> External LLM API
\end{lstlisting}

生产环境将 API 服务、任务 Worker、Redis、MySQL、MinIO
和向量库独立部署；LLM API
Key、数据库密码和对象存储密钥只通过环境变量或密钥管理系统注入。

\hypertarget{ux8bbeux8ba1ux8ffdux8e2aux77e9ux9635}{%
\section{设计追踪矩阵}\label{ux8bbeux8ba1ux8ffdux8e2aux77e9ux9635}}

\begin{longtable}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{\dimexpr(\linewidth-2.4cm-4\tabcolsep-3\arrayrulewidth)/1\relax}|}
\hline
需求类别 & 设计覆盖\tabularnewline
\hline
\endhead
文件全生命周期管理 & 文件管理模块、文件接口、文件表、对象存储设计、UI 工作台\tabularnewline
\hline
自然语言知识问答 & RAG 时序图、解析与向量化算法、知识库表、问答接口\tabularnewline
\hline
智能体任务编排 & Agent 时序图、ToolDefinition 类、工具接口、执行日志\tabularnewline
\hline
可视化工具流 & 工具流时序图、DAG 算法、Workflow 表、Vue Flow UI\tabularnewline
\hline
团队协作 & 团队接口、团队表、聊天/批注/通知 UI\tabularnewline
\hline
权限与安全 & RBAC/ACL 算法、权限表、权限接口、安全设计\tabularnewline
\hline
审计与可维护性 & 审计接口、审计日志表、结构化日志和服务分层\tabularnewline
\hline
\end{longtable}

\end{document}
